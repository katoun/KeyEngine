{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch KeyEditor",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/Binaries/Linux/KeyEditor",
            "args": [],
            "cwd": "${workspaceFolder}",
            "environment": [
                {
                    "name": "KEY_ENGINE_SDK_PATH",
                    "value": "${workspaceFolder}"
                }
            ],
            "MIMode": "gdb",
            "preLaunchTask": "cmake: build debug"
        }
    ]
}
